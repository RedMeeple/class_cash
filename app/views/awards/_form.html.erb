<%= simple_form_for @award do |f| %>

    I am going to give <%= f.grouped_collection_select :student_id, @periods, :students, :name, :id, :full_name %>

     the award for <%= f.association :award_type, label_method: :name, include_blank: true, label: false %>,

    because <%= f.input :reason, label: false %>.

    This award is going to come with an extra bonus of $<%= f.input :payment, label: false %>.

    <%= f.button :submit, "Give Award"%>
  
<% end %>
