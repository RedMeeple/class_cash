<p id="notice"><%= notice %></p>

<h1>Listing Periods</h1>

<table>
  <thead>
    <tr>
      <th>Instructor</th>
      <th>Payscale</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @periods.each do |period| %>
      <tr>
        <td><%= period.instructor_id %></td>
        <td><%= period.payscale %></td>
        <td><%= period.name %></td>
        <td><%= link_to 'Show', period %></td>
        <td><%= link_to 'Edit', edit_period_path(period) %></td>
        <td><%= link_to 'Destroy', period, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Enter Behavior Data', enter_behavior_periods_path %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Period', new_period_path %>
<%= link_to 'Home', root_path %>
