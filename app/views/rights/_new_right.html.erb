<%= simple_form_for StudentRightAssignment.find(right.id), url: assign_right_right_path(right.id), remote: true do |f| %>
  <%= f.hidden_field :right_id %>
  <%= f.hidden_field :student_id, value: right.student.id %>
<% end %>
