<%= simple_form_for StudentRightAssignment.find_by_student_id(student.id), url: assign_right_rights_path do |f| %>
  <%= f.association :right, label: false, label_method: :description %>

  <%= f.button :submit, "Assign Right" %>
<% end %>
