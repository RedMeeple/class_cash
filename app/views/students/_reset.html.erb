<%= form_tag reset_student_path(@student), remote: true do |f| %>

  <% @password = @student.make_password %>
  <%= hidden_field_tag :new_password, @password %>

  <%= submit_tag "Reset Password", class: "pure-button btn-cc reset-password-btn" %>
<% end %>
