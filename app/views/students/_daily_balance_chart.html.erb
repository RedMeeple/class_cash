<script>

  var data = <%= raw @daily_balances.to_json %>;
  var dates = [];
  var cashAmount = [];

  data.map(function (values) {
    dates.unshift(values[0]);
    cashAmount.unshift(values[1]);
  })

  app.students.cashChart(dates, cashAmount);

</script>
