<%= simple_form_for @job do |f| %>

  <%= f.grouped_collection_select :student_id, @periods, :students, :name, :id, :full_name %>
  <%= f.input :description %>
  <%= f.input :payscale %>

  <%= f.button :submit %>

<% end %>
