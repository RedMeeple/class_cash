<%= simple_form_for @job do |f| %>

  <%= f.association :student, label_method: :full_name, include_blank: true %>
  <%= f.input :description %>
  <%= f.input :payscale %>

  <%= f.button :submit %>

<% end %>
