
<h3>Welcome</h3>
<div>You have <%= @new_rights || "no" %> students who have earned a <%= link_to "new right", rights_path %>. </div>
<div><%= @transactions || "No" %> students have made <%= link_to "transactions", transactions_path %> today.</div>
<div><%= @loans || "No" %> students have made <%= link_to "loans", loans_path %> today.</div>
<div>
  Enter today's behavior for
  <% @unentered_periods.each do |up| %>
    <%= link_to "#{up.name}", enter_behavior_period_path(up) %>
  <% end %>
</div>
